<schemalist>
  <schema id="org.gnome.shell.extensions.taskicons" path="/org/gnome/shell/extensions/taskicons/">
    <key name="panel-box" type="s">
      <choices>
        <choice value="_leftBox"/>
        <choice value="_rightBox"/>
        <choice value="_centerBox"/>
      </choices>
      <aliases>
        <alias value="left" target="_leftBox"/>
        <alias value="right" target="_rightBox"/>
        <alias value="center" target="_centerBox"/>
      </aliases>
      <default>'_leftBox'</default>
      <summary>Panel Box</summary>
      <description>
        Which panel box to display the indicators.
      </description>
    </key>
    <key name="workspaces-before-active-position" type="mi">
      <default>1</default>
      <summary>Workspaces Before Active Position</summary>
      <description>
        Where to show, within the panel box, the indicators for workspaces
        before the active one.
        A value of "nothing" hides them.
      </description>
    </key>
    <key name="workspaces-after-active-position" type="mi">
      <default>1</default>
      <summary>Workspaces After Active Position</summary>
      <description>
        Where to show, within the panel box, the indicators for workspaces
        after the active one.
        A value of "nothing" hides them.
      </description>
    </key>
    <key name="active-workspace-position" type="mi">
      <default>1</default>
      <summary>Active Workspace Position</summary>
      <description>
        Where to show, within the panel box, the indicator for the active
        workspace.
        A value of "nothing" hides it.
      </description>
    </key>
    <key name="active-workspace-style-pseudo-class" type="s">
      <default>'active'</default>
      <summary>Active Workspace Style Pseudo Class</summary>
      <description>
        The CSS style pseudo class to be applied to the active workspace indicator.
      </description>
    </key>
    <key name="icons-style-class" type="s">
      <default>'app-menu-icon'</default>
      <summary>Icons Style Class</summary>
      <description>
        The CSS style class to be applied to the icons.
      </description>
    </key>
    <key name="icons-style" type="s">
      <default>''</default>
      <summary>Icons Style</summary>
      <description>
        The CSS style to be applied to the icons.
      </description>
    </key>
    <key name="icons-maximum-amount" type="mu">
      <default>nothing</default>
      <summary>Icons Maximum Amount</summary>
      <description>
        The maximum amount of window icons to display within each workspace
        indicator.
        A value of "nothing" removes the limit.
      </description>
    </key>
    <key name="icons-per-application" type="b">
      <default>true</default>
      <summary>Icons Per Application</summary>
      <description>
        Whether to group workspace icons by application.
      </description>
    </key>
    <key name="workspace-names-as-labels" type="b">
      <default>false</default>
      <summary>Workspace Names As Labels</summary>
      <description>
        Whether to use workspace names, instead of numbers, as labels.
        Names are set in "/org/gnome/desktop/wm/preferences/workspace-names".
      </description>
    </key>
    <key name="workspace-labels-position" type="mi">
      <default>0</default>
      <summary>Workspace Labels Position</summary>
      <description>
        Whether to display workspace labels.
        A negative value appends to the end.
        A value of "nothing" hides them.
      </description>
    </key>
  </schema>
</schemalist>
